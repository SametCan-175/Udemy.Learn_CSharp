namespace Udemy.Learn.D03.BooleanDegiskenInceleme
{
    internal class Program
    {
        static void Main(string[] args)
        {
            bool kullaniciAktif = true;
            bool urunAktif = false;

            Console.WriteLine(kullaniciAktif); // Evet
            Console.WriteLine(urunAktif);// Hayır (Olumsuzluk)
        }
    }
}
